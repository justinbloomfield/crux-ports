# Description: stacking window manager that cooks windows with orders from the Waitron 
# URL: https://github.com/tudurom/windowchef
# Maintainer: piecesofquiet < jbloomfield at live dot com >
# Depends on: 

name=windowchef
version=git-679a6b7
sversion=679a6b7
release=1
source=()
gitsource=https://github.com/tudurom/windowchef

build () {
      cd $PKGMK_SOURCE_DIR
      if cd $name; then
         git reset --hard
         git pull $gitsource
      else
         git clone $gitsource $name
      fi

      git checkout $sversion

      make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/man install
}