# Description: simple drag-and-drop source/sink for X or Wayland
# URL: https://github.com/mwh/dragon
# Maintainer: poq <jbloomfield at live dot com>
# Depends on: gtk3 

name=dragon
version=git-db7200f
sversion=db7200f
release=1
source=()

gitsource=https://github.com/mwh/dragon

build() {
    cd $PKGMK_SOURCE_DIR

    if cd $name; then
        git reset --hard
        git pull $gitsource
    else
        git clone $gitsource $name
        cd $name
    fi

    git checkout $sversion

    make DESTDIR=$PKG PREFIX=/usr MANPREFIX=/usr/man install
}
# vim: set ft=sh ts=4 et:
