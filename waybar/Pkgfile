# Description: Customisable Wayland bar for wlroots-based compositors
# URL: https://github.com/alexays/waybar
# Maintainer: poq <jbloomfield at live dot com>
# Depends on: gtkmm3 jsonpp wayland fmt spdlog gobject-introspection pulseaudio libnl libappindicator-gtk3 libmpdclient libdbusmenu-gtk3 libsndio wayland-protocols meson

name=waybar
version=git-13fda16
sversion=13fda16
release=1
source=()

gitsource=https://github.com/alexays/waybar

build() {
    cd $PKGMK_SOURCE_DIR

    if cd $name; then
        git reset --hard
        git pull $gitsource
    else
        git clone $gitsource $name
        cd $name
    fi
 
    git checkout $sversion
     


    meson build --prefix=/usr
    ninja -C build
    DESTDIR=$PKG ninja -C build install
}

# vim: set ft=sh ts=4 et:
