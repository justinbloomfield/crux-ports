# Description: GTK patchbay for PipeWire
# URL: https://gitlab.freedesktop.org/ryuukyu/helvum
# Maintainer: poq <jbloomfield at live dot com>
# Depends on:meson rust clang pipewire gtk4 

name=helvum
version=0.3.1
release=1
source=(https://gitlab.freedesktop.org/ryuukyu/$name/-/archive/$version/helvum-0.3.1.tar.gz)

build() {
    cd $name-$version

    meson setup build 
    cd build
    meson compile
    meson install --destdir=$PKG
}

# vim: set ft=sh ts=4 et:
